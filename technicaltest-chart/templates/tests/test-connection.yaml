apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "technicaltest-chart.fullname" . }}-test-connection"
  labels:
    {{- include "technicaltest-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "technicaltest-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
